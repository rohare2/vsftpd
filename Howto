If you want to allow /usr/sbin/vsftpd to be able to write to shared public content
Then you need to change the label on /var/ftp/pub to public_content_rw_t, and potentially turn on the allow_httpd_sys_script_anon_write boolean.
Do
# semanage fcontext -a -t public_content_rw_t /var/ftp/pub
# restorecon -R -v /var/ftp/pub
# setsebool -P allow_ftpd_anon_write 1
 
# Restrict ftps to a small set of ports
pasv_min_port=21000
pasv_max_port=21010

# enable TLS
rsa_cert_file=/etc/ssl/private/vsftpd.pem
rsa_private_key_file=/etc/ssl/private/vsftpd.pem
ssl_enable=YES
allow_anon_ssl=NO
force_local_data_ssl=YES
force_local_logins_ssl=YES
ssl_tlsv1=YES
ssl_sslv2=NO
ssl_sslv3=NO
require_ssl_reuse=NO
ssl_ciphers=HIGH

# don't verify certificate
$ cat ~/.lftp/rc
set ssl:verify-certificate no
